export type SalesItem = {
  month: string;
  sales: number;
}

export type SalesList = SalesItem[];

export type ExpenseItem = {
  category: string;
  amount: number;
}

export type ExpenseList = ExpenseItem[]
